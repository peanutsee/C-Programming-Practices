#include <stdio.h>

void main(){
    int num = 5; // num (variable) stores 5 (value)
    printf("num = %d\n", num);

    // Address Operator
    printf("&num = %p\n", &num); // prints the memory address of num (variable) with 5 (value)
}
